# Prompt Log — 2026-08-07

## Session: Stage 4 — Requests (form, persistence, requester dashboard)

### 12. Build Phase 4 (mentorship requests)

- **Task context:** Phases 0–3 done (foundations, data layer, Better Auth,
  mentor discovery). User noticed the "Become a mentor" button dead-ends:
  signed-in click → `/signup` → `(auth)` layout redirect → `/dashboard`
  under-construction placeholder. Asked for a Phase 4 plan, approved it,
  then said "work".
- **Raw prompt:** "lets plan phase 4" → plan presented → "work"
  (preceded by: "Its like we are not done with phase 3, cos i clicked on
  the become a mentor button, it takes me to dashboard for under
  construction page").
- **Interpretation:** Build the request flow per BUILD_PLAN steps 4.1–4.3
  (inline form on `/mentors/[id]`, single server Action with transaction,
  real `/dashboard` list) plus the session-aware landing CTA fix; verify
  with lint/tsc/build + live smoke on the throwaway :3100 server; sync all
  docs.
- **Refined execution prompt:** Files: `lib/validation/request.ts`
  (zod v4: message 1–2000, optional skill), `lib/actions/requests.ts`
  (`createMentorshipRequest`: `requireUser` → mentor lookup → self-request
  block → skill-membership check → duplicate-pending check + insert in one
  interactive transaction, `{ error, success }` state),
  `components/request-form.tsx` (`useActionState`, textarea + skill
  select, success branch), wire through `request-cta.tsx` + profile page
  (skills with ids), `app/dashboard/page.tsx` (own requests, badges,
  empty state), `app/page.tsx` (session-aware "Become a mentor").
  Verify: lint + build + tsc; smoke on `BETTER_AUTH_URL=http://localhost:3100
  next start -p 3100` covering create/duplicate/self/invalid-skill/unknown-
  mentor/message-length/dashboard per user/CTA states; cleanup DB rows +
  sessions + server + temp files; sync BUILD_PLAN/PROGRESS/PROJECT_BUILD/
  prompt log.
- **Final execution prompt:** Same as refined. Server actions tested from
  curl by replaying the form's hidden `$ACTION_*` fields extracted from
  the page HTML (multipart POST to the page URL). DB state checked via
  `pg` (generated Prisma client can't be `require`d by plain node — ESM
  extensionless imports).
- **Outcome:** ✅ Completed. All 6 files built as planned; lint/tsc/build
  clean. Smoke results: create OK (row `pending`), same-skill duplicate
  rejected, different/no-skill allowed (3 rows), self-request blocked,
  fabricated skill rejected, unknown mentor rejected, 2001-char message
  rejected, missing skillId accepted after fix, dashboards correct
  (priya 3 cards, tunde empty), anon/signed-in CTAs + landing hrefs
  correct. Cleanup done (3 requests + 3 sessions by id, server killed).
  Docs synced: BUILD_PLAN 4.1–4.3 rewritten + 4.4 added, PROGRESS 14/18
  phase 5, PROJECT_BUILD #10, this log. Lesson: zod `.optional()` does
  not accept `null` from `FormData` — union with `literal("")`/`null`
  (bug found in verification); `$ACTION_KEY` is per-page; `next start`
  serves the last build; `now()`-based session cleanup must use explicit
  ids or timestamps from the test window.

### 13. Multi-skill requests (UUID-in-select fix + join table)

- **Task context:** Follow-up bug report on Stage 4: "when i select a
  skill it shows the skill uuid it should be skill name instead, and we
  should be able to select more than one skill at a time" (with a
  screenshot the model could not read — informed the user).
- **Raw prompt:** as above.
- **Interpretation:** Two changes: (1) the skill picker shows the raw
  UUID — base-ui `SelectValue` falls back to the raw `value` when it
  cannot resolve an item's label (invisible in mentor-filters because its
  values were skill names); (2) multi-skill selection — needs a
  `mentorship_request_skills` join table (one request, N skills).
- **Refined execution prompt:** Migration `20260807150000_add_mentorship_request_skills`
  (join table: UUID PK, RESTRICT/NO ACTION FKs, unique `(request_id,
  skill_id)`, index `skill_id`; backfill then drop `skill_id` + its FK);
  schema.prisma models; validation `skillIds` array; Action reads
  `formData.getAll("skillIds")`, batch-validates mentor offerings,
  per-skill duplicate check naming the skill, request + `createMany` in
  one transaction; checkbox chips replace the Select; dashboard joins
  skill names. Verify lint/tsc/build + smoke on throwaway :3100 + docs
  sync (BUILD_PLAN 4.1/4.2 + DB tables, PROGRESS follow-up, PROJECT_BUILD
  #11, this log).
- **Final execution prompt:** Same as refined; DB checks via `pg` (as
  before); cleanup deletes join rows before requests (RESTRICT FK) and
  test sessions by id (better-auth `created_at` lags DB `now()` — time
  windows miss them).
- **Outcome:** ✅ Completed. Checkbox chips render names as labels, UUIDs
  only in values; multi-skill create → 1 request + 2 join rows; per-skill
  duplicate rejected by name; no-skill and invalid-skill paths correct;
  `skill_id` column dropped; dashboard shows sorted joined names (2 rows
  SQL-verified); cleanup done (2 join rows + 2 requests + 1 session by
  id; server killed; temp files removed). One tsc hiccup: the
  `$transaction` callback return union didn't narrow `existing.message` —
  fixed with `as const` on the discriminant. Docs synced. Lessons: base-ui
  SelectValue falls back to raw value — avoid non-displayable item values;
  better-auth session created_at lags DB clock — delete test sessions by
  id; RESTRICT FKs require deleting join rows before parents.

### 14. Action-based queries + Stage 5.1 (mentor profile create/edit)

- **Task context:** Two user requests after the multi-skill fix: (1) move
  DB interaction off the mentors page into an action; (2) "become a
  mentor" leads to a placeholder — no profile creation or mentorship
  details form (Phase 5.1 was still planned, not built).
- **Raw prompt:** "on the mentors page, the data base iteration should be
  in the action not on the page / also i clicked on the become a mentor
  button, its not creating a mentor profile for me neither allow me to
  enter my mentorship details"
- **Interpretation:** Refactor `/mentors` (and `/mentors/[id]`, same
  principle) to call read actions; build Step 5.1 end-to-end
  (upsert Action, form, page) so signed-in users without a profile can
  create one and existing users can edit.
- **Refined execution prompt:** `lib/actions/mentors.ts` (server-only
  reads: getMentorDirectory with moved where-builder, getMentorProfile);
  thin pages. `lib/validation/profile.ts`; `lib/actions/profiles.ts`
  (getProfileEditor + upsertMentorProfile: requireUser → parse →
  catalog check → one transaction with deleteMany+createMany skill
  replace); `components/mentor-profile-form.tsx` (chips pre-checked on
  edit); `app/(mentor)/profile/page.tsx` create/edit copy. Verify
  lint/tsc/build + smoke :3100 (fresh signup → create form → create with
  2 skills → directory + skill filter visibility → edit prefill → replace
  skills → validation rejects) + cleanup in RESTRICT-FK order + docs
  sync (BUILD_PLAN 5.1, PROGRESS 15/18, PROJECT_BUILD #12, this log).
- **Final execution prompt:** Same as refined. Smoke user
  smoke-p5@example.com created via sign-up API, deleted after (skills →
  profile → session → account → user).
- **Outcome:** ✅ Completed. Directory + profile pages now query through
  the actions layer. Create verified (row + 2 mentor_skills + success
  UI), edit verified (prefilled chips via `checked=""`, skills replaced
  atomically, bio/years updated), validation edges pass (short bio,
  empty years, 100). Cleanup complete. Docs synced. Lessons: `z.coerce.
  number()` maps missing/empty FormData to 0 (silently valid) — use
  regex-transform instead; bash expands `$1` inside double-quoted
  `node -e` scripts — single-quote them; a 10-char string passes
  `min(10)` — mind test-string lengths.

### 15. Stage 5.2 — Inbox & respond + header navigation fix

- **Task context:** "there is no way for users to go to the profile to
  see all of the requests they have pending". Root gaps: desktop nav only
  ever linked Find Mentors (dashboard/profile/inbox were mobile-sheet
  only), and /inbox was a placeholder — mentors could not see incoming
  pending requests.
- **Raw prompt:** "there is not way for usrs to go to the profile to see
  all of the request they have pending"
- **Interpretation:** Two fixes: (1) header nav links for signed-in users
  (My Requests / Mentor Profile / Inbox — Inbox only when the user has a
  mentor profile, matching the requireMentorProfile redirect); (2) build
  Step 5.2: real inbox listing incoming requests with accept/decline,
  ownership-enforced, decided_at written once.
- **Refined execution prompt:** getInbox(mentorProfileId) server-only
  read (pending first, then decided, newest first); respondToRequest
  "use server" action: zod → requireUser → ownership check → optimistic
  updateMany({id, status:"pending"}) setting status+decidedAt, count 0 →
  "already responded"; per-row InboxRequestActions client component
  (Accept/Decline buttons named `decision`, hidden requestId,
  useActionState); inbox page via requireMentorProfile; nav links via new
  isMentor() DAL helper; also move /dashboard to the actions layer
  (getMyRequests) and fix anonymous-undefined requesterId filter leak
  (requireUser). Verify lint/tsc/build; smoke on :3100: 3 pending
  requests in Amara's inbox, accept, decline, re-respond block,
  cross-mentor ownership replay, empty state for a mentor with no
  requests; restore Babatunde's request untouched; cleanup smoke rows;
  docs sync (BUILD_PLAN 5.2, PROGRESS 16/18 phase 6, PROJECT_BUILD #13,
  this log).
- **Final execution prompt:** Same as refined; smoke requesters seeded
  via direct SQL inserts (createMentorshipRequest already verified) with
  a one-transaction insert after the first attempt left partial rows
  (users.updated_at / requests.updated_at NOT NULL, no default).
- **Outcome:** ✅ Completed. All smoke flows pass (accept → badge +
  "You accepted this request on Aug 7, 2026" + decided_at; decline;
  re-respond error; Priya replay → ownership error, no mutation; empty
  state; non-mentor redirect). Babatunde's request restored untouched.
  Smoke rows deleted, :3100 killed, docs synced. Lessons: `$ACTION_KEY`
  is session-scoped (not per-render) and reusable in replays; SSR text
  across JSX expressions is split by `<!-- -->` comment nodes — strip
  before string assertions; `where: { id: undefined }`-style Prisma
  filters silently drop (the dashboard leak root cause — fix by
  requireUser, never rely on optional ids in where clauses).

### 16. Stage 6.4 — DB-driven notifications (page + header bell)

- **Task context:** User asked, in addition to Phase 6, for a
  DB-driven notification page, and asked to see the proposed table first
  ("lets me see your propsed db table"). Design (one notifications table,
  RESTRICT FK, type CHECK, frozen title/body/link, nullable read_at,
  writes inside the triggering actions' transactions) approved via
  question tool ("Build it (page + bell)").
- **Raw prompt:** "in addtion to the phase 6, also add a db driven
  notification page, we can have the lets me see your propsed db table"
- **Interpretation:** (1) show the proposed table + decisions and get
  approval; (2) on approval: migration, notification writes in
  createMentorshipRequest (→ mentor, request_received, link /inbox) and
  respondToRequest (→ requester, request_accepted|declined, link
  /dashboard) inside the same transactions; reads/writes module
  lib/actions/notifications.ts; /notifications page (unread rows = tinted
  submit buttons with dot, read rows = Links, mark-all button, empty
  state); nav bell with unread badge (desktop) + link (mobile).
- **Refined execution prompt:** Migration hand-written forward-only
  (20260807160000_add_notifications) + schema model + migrate deploy +
  generate; writes atomic with mutations (interactive tx — array-form
  $transaction would create phantom notifications on stale replays);
  markNotificationRead = hidden notificationId+link, userId-scoped
  updateMany, redirect(link); unread-first ordering via Prisma nulls
  first; verify lint/tsc/build; smoke :3100 full flow (sign-up → request
  form → Amara bell/page unread → mark-all → accept → requester
  notification → row-click mark-read redirect + read_at) then clean
  RESTRICT-order, restore Babatunde's request untouched; docs sync
  (BUILD_PLAN 6.4, PROGRESS 17/20, PROJECT_BUILD #14, this log).
- **Final execution prompt:** Same as refined; curl replays must use
  MSYS_NO_PATHCONV=1 for -F values starting with "/" (git-bash rewrote
  link=/dashboard into C:/Program Files/Git/dashboard — schema correctly
  rejected it; the mangled cookie-jar path afterwards looked like a
  session loss; diagnose via 303 Location + temporary console.log of the
  parsed values in the action).
- **Outcome:** ✅ Completed. Migration deployed + client generated.
  Full smoke flow passed: bell badge/aria-label counts, unread rows,
  mark-all, accepted notification to requester, row-click 303 →
  /dashboard with read_at set, read-row re-render. Cleanup complete
  (sessions FK reminder), Babatunde's request untouched, :3100 killed,
  lint/tsc clean. Lessons: array-form $transaction runs statements
  unconditionally (use interactive when later statements depend on an
  earlier result); MSYS_NO_PATHCONV=1 for /-prefixed -F values; diagnose
  redirects by the Location header (parse-fail redirect ≠ success
  redirect).

### 17. Fix: stale Prisma client on the dev server (repeat incident)

- **Task context:** After the notifications feature shipped, the user
  hit `Cannot read properties of undefined (reading 'count')` in
  `getUnreadNotificationCount` (Nav) on :3000.
- **Raw prompt:** Console error paste (stack: notifications.ts:44 → Nav).
- **Interpretation:** Classic stale-client: the :3000 dev server predates
  the notifications migration/generate; `prisma.notification` is
  undefined in its in-memory client (26 occurrences in .dev-server.log
  confirmed it was serving the error). The :3100 smoke server (fresh
  start) had proven the client itself is fine.
- **Refined execution prompt:** Confirm diagnosis via log grep → kill
  :3000 PID → nohup npm run dev → verify signed-in `/` renders (nav
  exercises the failing query) → docs entries.
- **Final execution prompt:** Same; verified sign-in 200 + home 200 +
  nav text present + no error string in body.
- **Outcome:** ✅ Restart fixed it; repeat rule documented in PROGRESS:
  restart long-running dev servers after prisma generate.

### 18. Fix: Vercel build failure (prisma generate in postinstall)

- **Task context:** First Vercel deploy failed at install:
  `PrismaConfigEnvError: Cannot resolve environment variable:
  DATABASE_URL` from the postinstall `prisma generate`.
- **Raw prompt:** Vercel build log paste ending "Command npm install
  exited with 1".
- **Interpretation:** Vercel runs `npm install` without project env vars;
  `prisma.config.ts` `env("DATABASE_URL")` throws. Standard fix: run
  `prisma generate` in the build script instead of postinstall.
- **Refined execution prompt:** package.json: remove postinstall, build =
  "prisma generate && next build"; verify from clean state (rm
  lib/generated → npm run build); docs entries.
- **Final execution prompt:** Same; verified clean build passes.
- **Outcome:** ✅ Fixed. Remaining deployment notes documented: set
  DATABASE_URL on Vercel; production DB must be reachable from Vercel
  (local EnvKit Postgres is not) and migrated with `prisma migrate
  deploy`.
