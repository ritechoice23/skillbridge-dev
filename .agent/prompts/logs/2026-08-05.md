# Prompt Log — 2026-08-05

## Session: Project planning & documentation system

**Timestamps:** 2026-08-05

### 1. Add prompt-logging config + build-story rule

- **Task context:** Fresh Next.js 16 project (`skillbridge-dev`); global rule
  requires a `prompt_logging` config in AGENTS.md (default `enabled: false`);
  user wanted a rule that maintains a project build-story file.
- **Raw prompt:** "I want you to add the config for prompt logging to the
  agents.md. Then I also want you to add something to the agents.md that will
  create another file... a mega file that documents the entire stories step by
  step of building the project and the decisions along the way."
- **Interpretation:** Add `prompt_logging` config; add a section to AGENTS.md
  defining `PROJECT_BUILD.md` — a chronological build-story reference file;
  create and seed the file.
- **Refined execution prompt:** Add `prompt_logging: enabled: false` config
  block at the top of AGENTS.md (keeping the nextjs-agent-rules block intact);
  append a "Project Build Documentation" section specifying content, update
  cadence, and role of `PROJECT_BUILD.md`; create `PROJECT_BUILD.md` with
  header/rules and entries for project init and the docs system.
- **Final execution prompt:** Same as refined; execute without prompting for
  further input, then report.
- **Outcome:** ✅ Completed. AGENTS.md updated (config + section);
  `PROJECT_BUILD.md` created with 2 entries. No validation beyond file reads.
  Note: the user later committed a change that set `prompt_logging: enabled:
  true` and removed the Project Build Documentation section.

### 2. Document PRD, build plan, and tracker

- **Task context:** User asked to document the PRD in `docs/`, plan the
  project step-by-step, create a dedicated plan file and a tracking file.
  `docs/PRD.md` existed but was empty (0 lines); product was undefined.
- **Raw prompt:** "So, you need to document the PRD of this project inside the
  docs folder... create a dedicated file for that step-by-step documentation
  and a file for us to be able to track what is going on."
- **Interpretation:** Write the PRD from scratch, then produce a phased
  step-by-step build plan and a status tracker, both in `docs/`. Product
  details needed clarification first.
- **Refined execution prompt (after clarification):** Product = SkillBridge:
  learners discover mentors for practical skills and send mentorship requests.
  General public; MVP-first. Decisions: real mentor accounts, requests stored
  in an in-app inbox, PostgreSQL storage. Build the docs system:
  `docs/PRD.md`, `docs/BUILD_PLAN.md` (steps with objective/tasks/files/
  acceptance criteria/decisions + full database design), `docs/PROGRESS.md`
  (status table + session log), extend AGENTS.md with a Project Docs System
  rule. Database design must follow global principles: UUID PKs everywhere,
  restrictive FKs (no cascades), forward-only migrations, explicit cleanup in
  application code.
- **Final execution prompt:** Same as refined; defaults proposed and
  approved: Prisma ORM, Auth.js, learner+mentor roles, Zod validation.
- **Outcome:** ✅ Completed. Wrote `docs/PRD.md` (12 sections incl. user
  stories, FRs, NFRs, data model overview), `docs/BUILD_PLAN.md` (6 phases,
  17 steps, full DB design: 5 tables, all UUID PKs, RESTRICT FKs, CHECK
  enums, index list, design-decision rationale), `docs/PROGRESS.md` (status
  legend, step table, session log). Appended "Project Docs System" section to
  AGENTS.md (preserving the existing nextjs-agent-rules block and the
  user's `enabled: true` setting). Created `.agent/prompts/logs/2026-08-05.md`
  per enabled logging. Validation: file reads; no code executed this session.
  Unresolved: user previously removed the Project Build Documentation section
  from AGENTS.md in a commit while keeping `PROJECT_BUILD.md` — flagged for
  user decision on whether to restore the rule.

### 3. Execute Stage 0 with shadcn

- **Task context:** User approved Stage 0 plan (foundations), then added the
  directive to use shadcn for all components instead of custom UI.
- **Raw prompt:** "lets plan out stage 1" → replan asked Stage 0 first;
  final: "we need to used shadcn for all components, no need to re-invent the
  wheel" + "work".
- **Interpretation:** Execute Phase 0: shadcn init (user chose base-nova),
  layout shell, routing structure; verify; sync docs.
- **Refined execution prompt:** init shadcn (base + nova), add button/badge/
  card/separator/sheet/skeleton; fix globals.css font wiring + indigo brand;
  build Nav/Footer/landing/404 from shadcn components; create placeholder
  routes `/mentors`, `/mentors/[id]`, `(auth)/signup|login`, `/dashboard`,
  `(mentor)/profile|inbox` with typed props; verify lint/tsc/build/smoke;
  sync PROGRESS/BUILD_PLAN/PROJECT_BUILD.
- **Final execution prompt:** Same as refined; handle the actual preset name
  (`--base base --preset nova`, since `base-nova` was rejected by the CLI).
- **Outcome:** ✅ Completed. shadcn init + 6 components; indigo primary;
  dark-mode script; Nav/Footer/landing/404; 9 new pages + 2 segment layouts +
  shared Placeholder. Validation: eslint clean, tsc clean (after typing
  route-group layouts manually — `LayoutProps` rejects group paths), next
  build OK (10 routes), dev smoke test all 200 / unknown 404 / no stock
  content. Docs: PROGRESS 3/18 (Phase 1 next), BUILD_PLAN gained step 0.0 +
  reality sync, PROJECT_BUILD entry #4. Lesson: shadcn preset names changed —
  style presets (nova/vega/…) and base (base/radix/aria) are separate flags.

### 4. Theme: landmark green

- **Task context:** Follow-up to Stage 0 — user wants the shadcn primary to be
  a green close to "landmark green".
- **Raw prompt:** "change the shadcn theme to green that is close to landmark green"
- **Interpretation:** Swap the `--primary` token (light + dark) in
  app/globals.css from indigo to a deep green; keep semantic tokens; ensure
  contrast.
- **Refined execution prompt:** No official Landmark Group green exists
  (brand is gold/blue), so use the classic deep brand green #00875A (close to
  Pantone 348C). Convert to oklch, verify white-text contrast ≥ AA, set dark
  mode to a lighter hue-matched green, verify compiled CSS, sync docs.
- **Outcome:** ✅ Completed. Light `--primary: oklch(0.551 0.122 161.179)`
  (#00875A, 4.55:1 AA with white text); dark `--primary: oklch(0.765 0.177
  163.223)` (#00d294, ~7.6:1). Verified compiled dev CSS contains both values
  and page returns 200. Docs: BUILD_PLAN 0.0/0.1 decisions updated, PROGRESS
  session log amended. Lesson: oklch→hex round-trips differ slightly from
  source hex (expected, browser conversion).

### 5. Role model pivot: no fixed roles

- **Task context:** The plan had users choosing learner|mentor at sign-up.
  User wants an open model: one authenticated user can be both — a learner
  to someone, a mentor to another ("they can just be discovered as it is").
- **Raw prompt:** "The way the application is planned is that there's going
  to be users that can be mentor or another one that can be learner. I want
  us to make it open in such a way that an authenticated user can play any
  of the roles. A learner to someone can also be a mentor to another, so
  they can just be discovered as it is."
- **Interpretation:** Remove the fixed-role concept everywhere: database
  (no role column), auth (no role picker), guards (mentor = has profile),
  docs (PRD/BUILD_PLAN/PROGRESS), and signup placeholder copy.
- **Refined execution prompt:** Update docs/PRD.md (5.3 role model, US-01/02,
  FR-AUTH-2/3, FR-REQUEST-1, FR-PROFILE-1, overview, open questions),
  docs/BUILD_PLAN.md (users table, design decisions, step 2.1/2.2/4.x/5.1,
  phase table, indexes), docs/PROGRESS.md (step names + decisions + session
  entry), PROJECT_BUILD.md entry, app/(auth)/signup/page.tsx copy. Derive
  mentor status from mentor_profiles existence; keep request status CHECK.
  Validate with lint + tsc.
- **Outcome:** ✅ Completed. All docs consistent; signup placeholder updated;
  lint + tsc clean. Lesson: role is derived state — store profile existence,
  not a role flag, so "both roles" is free and can't desync.

### 6. Stage 1: Data layer

- **Task context:** Build mode. Approved plan for steps 1.1–1.3 (DB setup,
  Prisma schema & forward-only migration, seed) on top of the no-fixed-roles
  model and global DB principles (UUID PKs, restrictive FKs, no cascades).
- **Raw prompt:** "lets work on stage 1"
- **Interpretation:** Execute the planned data layer: create the Postgres DB
  via EnvKit, wire env, install Prisma 7 + pg adapter, schema with UUIDs and
  no cascades, forward-only migration with CHECKs, idempotent seed, verify
  against the live DB, sync docs.
- **Refined execution prompt:** (1.1) envkit_create_db postgres skillbridge;
  .env DATABASE_URL. (1.2) npm i @prisma/client@7 @prisma/adapter-pg pg
  bcryptjs; dev prisma@7 tsx dotenv; prisma.config.ts; schema (no role, UUID
  defaults, Restrict+NoAction FKs, @map snake_case, @@index); migration
  --create-only then append CHECKs then apply; lib/db.ts singleton; next
  config serverExternalPackages; gitignore generated; postinstall generate.
  (1.3) seed.ts idempotent transactional; run twice. Verify live schema via
  information_schema, FK/CHECK raw-SQL demos, lint/tsc/build; sync docs.
- **Final execution prompt:** Same as refined; also explicitly set
  `onUpdate: NoAction` on all relations after noticing Prisma's default
  ON UPDATE CASCADE violates the global no-cascades rule.
- **Outcome:** ✅ Completed. DB created; migration applied; all 6 FKs
  RESTRICT/NO ACTION; CHECKs verified (23514); FK RESTRICT verified (23503);
  seed idempotent (4 users/4 profiles/12 skills/9 mentor_skills); migrate
  status clean; lint/tsc/build clean. Docs: PROGRESS 6/18 → Phase 2,
  BUILD_PLAN 1.1–1.3 synced, PROJECT_BUILD #6. Lesson: Prisma defaults
  ON UPDATE CASCADE on every FK — declare `onUpdate: NoAction` in schema to
  comply with no-cascade rules without hand-editing migrations.

### 7. Remove headline from mentor profile

- **Task context:** Follow-up correction on Stage 1 schema, filed as a line
  comment on `prisma/schema.prisma` line 27 (`headline` field).
- **Raw prompt:** "remove headline from the profile"
- **Interpretation:** Drop `mentor_profiles.headline` end-to-end: schema,
  seed, forward-only migration, docs (BUILD_PLAN table + steps 3.1/3.2/3.3/5.1,
  PRD US-02/FR-DISCOVERY-2/3/FR-PROFILE-1), verify against live DB.
- **Refined execution prompt:** (1) Remove field from schema; (2) drop
  headline from seed mentors + upserts; (3) new migration dropping the
  column, applied forward-only; (4) verify: columns via pg, seed twice,
  lint/tsc/build; (5) docs sync (BUILD_PLAN, PRD, PROJECT_BUILD amendment,
  prompt log).
- **Final execution prompt:** Same as refined; when `migrate dev` refused on
  the data-loss warning in a non-interactive shell (even with `yes |`), and
  `migrate diff --from-migrations` required a shadowDatabaseUrl not set —
  generate the drop SQL by hand (matches Prisma output) and apply via
  `migrate deploy`.
- **Outcome:** ✅ Completed. Migration `20260805123000_drop_mentor_profile_headline`
  applied; live columns now id, user_id, bio, experience_years, created_at,
  updated_at; seed idempotent (4/4/12/9); migrate status up-to-date; lint +
  tsc + build clean. Docs synced. Lesson: Prisma `migrate dev` aborts in
  non-interactive shells when a data-loss warning needs confirmation — hand
  write the migration file and use `migrate deploy`; `migrate diff
  --from-migrations` needs `datasource.shadowDatabaseUrl` configured.

### 8. Stage 2: Authentication (Better Auth)

- **Task context:** Build mode after Stage 1. Planning request ("lets plan
  stage 1", actually stage 2) → auth research (Auth.js v5 beta; project
  absorbed into Better Auth) → user chose **Better Auth + database
  sessions**; user also asked to drop the now-unused `users.password_hash`.
  Execution: "work" + "lets continue stage 2".
- **Raw prompt:** "lets plan stage 1" … "as a result of changing to better
  auth, drop db column we may not need again" … "work".
- **Interpretation:** Replace Auth.js v5 with Better Auth 1.6.26 (database
  sessions): migration (users −password_hash, +email_verified/image;
  accounts/sessions/verifications), auth core + API route + nextCookies,
  Zod-validated server actions, signup/login forms, proxy guards + DAL,
  session-aware nav, seed credential accounts; verify; sync docs.
- **Refined execution prompt:** (1) install better-auth@1.6.26 zod@4.4.3 +
  shadcn input/label; (2) migration `20260805130000_add_better_auth` —
  hand-written SQL (migrate dev refuses non-interactive on data loss) +
  `migrate deploy` + generate; (3) `lib/auth.ts` (prismaAdapter postgresql,
  model mapping, generateId uuid, custom bcrypt hash/verify, nextCookies),
  `lib/auth/password.ts`, `/api/auth/[...all]` route; (4) `lib/actions/auth.ts`
  (zod v4 z.email, policy, signUp/signIn/signOut, redirect /dashboard);
  (5) `(auth)/signup|login` pages + forms (useActionState); (6) `proxy.ts`
  (protected + auth-route redirects, matcher), `lib/auth/dal.ts`
  (getSession cache, requireUser, requireMentorProfile), layout guards,
  nav; (7) seed accounts; (8) verify migrate status, live schema (8 FKs
  RESTRICT/NO ACTION, no password_hash), seed twice, lint, **build before
  tsc** (stale `.next` types); (9) curl smoke on throwaway server.
- **Final execution prompt:** Same as refined; verify with
  `node -r dotenv/config -e` (plain node lacks .env); use a separate
  `next start -p 3100` for curl flows (user dev server :3000 untouched);
  CSRF: curl mutations need `Origin` header.
- **Outcome:** ✅ Completed. Migration applied, live schema verified (users:
  id, email, name, created_at, updated_at, email_verified, image; 8 FKs no
  cascade); lint/tsc/build clean; seed idempotent (4/4/12/9/4). Live curl
  flows all passed on :3100 (proxy 307s, sign-up 200 + cookie + sessions
  row, dup email 422, wrong pw 401, demo mentor bcrypt sign-in 200,
  signed-in /login → /dashboard, sign-out 200 + row deleted, get-session
  null; 400 without Origin; 23503 deleting user with live session). Test
  server killed, temp artifacts removed. Docs synced (BUILD_PLAN 2.1/2.2 +
  DB design tables + phase table, PROGRESS 8/18 phase 3, PRD stack table,
  PROJECT_BUILD #7). Lessons: `next start` on a separate port is a safe
  test harness (dev server guard only blocks second `next dev`); Better
  Auth sign-out/sign-up require Origin (CSRF) + JSON content-type; stale
  `.next/dev/types` breaks tsc until a build regenerates them.

### 9. Document the auth implementation

- **Task context:** After Stage 2 verification, user asked for a
  step-by-step explanation of the auth/session implementation, then
  "document it" — persist that explanation in the project docs.
- **Raw prompt:** "explain step by step with code/file ref how you
  implemented the authentication and session handling" … "document it".
- **Interpretation:** Create a dedicated technical reference `docs/AUTH.md`
  in the existing docs system; cross-link from BUILD_PLAN; update
  PROGRESS/PROJECT_BUILD/prompt log per the docs-maintenance rules.
- **Refined execution prompt:** Read all auth files to ground the write
  (lib/auth.ts, lib/auth/password.ts, lib/actions/auth.ts, lib/auth/dal.ts,
  api route, proxy.ts, forms, layout guards, nav, seed). Write `docs/AUTH.md`
  as a walkthrough: architecture diagram, instance config table, hashing,
  schema storage, API route, server actions + error mapping, forms, DAL
  guards, two-layer protection (proxy vs DAL), cookie mechanics
  (token.signature, HttpOnly, SameSite, 7-day, Origin CSRF), seed/demo
  accounts, verified end-to-end flow table, configuration, testing recipes
  (throwaway :3100 server, curl headers, dotenv, build-before-tsc).
- **Final execution prompt:** Same as refined; link BUILD_PLAN 2.1 → AUTH.md,
  append PROGRESS follow-up note + prompt log entry, amend PROJECT_BUILD #7.
- **Outcome:** ✅ Completed. `docs/AUTH.md` created; BUILD_PLAN 2.1
  reference added; PROGRESS follow-up appended; PROJECT_BUILD #7 amended;
  prompt log appended. No code changed, so no lint/build validation needed —
  docs-only session.
