# Prompt Log — 2026-08-05

## Session: Project planning & documentation system

**Timestamps:** 2026-08-05

### 1. Add prompt-logging config + build-story rule

- **Task context:** Fresh Next.js 16 project (`skillbridge-dev`); global rule
  requires a `prompt_logging` config in AGENTS.md (default `enabled: false`);
  user wanted a rule that maintains a project build-story file.
- **Raw prompt:** "I want you to add the config for prompt logging to the
  agents.md. Then I also want you to add something to the agents.md that will
  create another file... a mega file that documents the entire stories step by
  step of building the project and the decisions along the way."
- **Interpretation:** Add `prompt_logging` config; add a section to AGENTS.md
  defining `PROJECT_BUILD.md` — a chronological build-story reference file;
  create and seed the file.
- **Refined execution prompt:** Add `prompt_logging: enabled: false` config
  block at the top of AGENTS.md (keeping the nextjs-agent-rules block intact);
  append a "Project Build Documentation" section specifying content, update
  cadence, and role of `PROJECT_BUILD.md`; create `PROJECT_BUILD.md` with
  header/rules and entries for project init and the docs system.
- **Final execution prompt:** Same as refined; execute without prompting for
  further input, then report.
- **Outcome:** ✅ Completed. AGENTS.md updated (config + section);
  `PROJECT_BUILD.md` created with 2 entries. No validation beyond file reads.
  Note: the user later committed a change that set `prompt_logging: enabled:
  true` and removed the Project Build Documentation section.

### 2. Document PRD, build plan, and tracker

- **Task context:** User asked to document the PRD in `docs/`, plan the
  project step-by-step, create a dedicated plan file and a tracking file.
  `docs/PRD.md` existed but was empty (0 lines); product was undefined.
- **Raw prompt:** "So, you need to document the PRD of this project inside the
  docs folder... create a dedicated file for that step-by-step documentation
  and a file for us to be able to track what is going on."
- **Interpretation:** Write the PRD from scratch, then produce a phased
  step-by-step build plan and a status tracker, both in `docs/`. Product
  details needed clarification first.
- **Refined execution prompt (after clarification):** Product = SkillBridge:
  learners discover mentors for practical skills and send mentorship requests.
  General public; MVP-first. Decisions: real mentor accounts, requests stored
  in an in-app inbox, PostgreSQL storage. Build the docs system:
  `docs/PRD.md`, `docs/BUILD_PLAN.md` (steps with objective/tasks/files/
  acceptance criteria/decisions + full database design), `docs/PROGRESS.md`
  (status table + session log), extend AGENTS.md with a Project Docs System
  rule. Database design must follow global principles: UUID PKs everywhere,
  restrictive FKs (no cascades), forward-only migrations, explicit cleanup in
  application code.
- **Final execution prompt:** Same as refined; defaults proposed and
  approved: Prisma ORM, Auth.js, learner+mentor roles, Zod validation.
- **Outcome:** ✅ Completed. Wrote `docs/PRD.md` (12 sections incl. user
  stories, FRs, NFRs, data model overview), `docs/BUILD_PLAN.md` (6 phases,
  17 steps, full DB design: 5 tables, all UUID PKs, RESTRICT FKs, CHECK
  enums, index list, design-decision rationale), `docs/PROGRESS.md` (status
  legend, step table, session log). Appended "Project Docs System" section to
  AGENTS.md (preserving the existing nextjs-agent-rules block and the
  user's `enabled: true` setting). Created `.agent/prompts/logs/2026-08-05.md`
  per enabled logging. Validation: file reads; no code executed this session.
  Unresolved: user previously removed the Project Build Documentation section
  from AGENTS.md in a commit while keeping `PROJECT_BUILD.md` — flagged for
  user decision on whether to restore the rule.
